<?xml version="1.0"?>
<configuration>
	<connectionStrings>
		<!-- please adjust the connection string embedded in the element below to target the proper catalog / server using the proper user / password combination -->
		<add name="ConnectionString.SQL Server (SqlClient)" connectionString="data source=(localdb)\MSSQLLocalDB;initial catalog=AdventureWorks2016;integrated security=SSPI;persist security info=False;packet size=4096"/>
	</connectionStrings>
</configuration> 